| Feature | Test Case | Steps to execute test case | Expected Output | Actual Output | Status | More Information |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **Peer Discovery** | Valid Peer Discovery (mDNS) | 1. Open the Stoa application on Device A.<br>2. Open the Stoa application on Device B connected to the same Wi-Fi network.<br>3. Open the "Peers" tab on Device A.<br>4. Wait for up to 5 seconds. | Device B should appear in Device A's peer list with its correct Username and "Online" status. | Device B appears in Device A's peer list with the correct Username and "Online" status. | Pass | No errors in console regarding mDNS resolution. IP addressing is correctly mapped in the background `DiscoveryService`. |
| **P2P Direct Messaging** | Send Text Message | 1. Tap on a discovered peer (Device B) in the Peers list on Device A.<br>2. Type "Hello World" in the message input field.<br>3. Tap the "Send" button. | The message "Hello World" should instantly appear on Device B's chat screen. | The message "Hello World" instantly appears on Device B's chat screen. | Pass | Verify in the local Drift SQLite database on both devices that the message was saved with the correct `timestamp` and `senderId`. |
| **P2P File Transfer** | Send Image File | 1. Open a direct chat with Device B on Device A.<br>2. Tap the attachment icon.<br>3. Select an image file (e.g., `.png` < 5MB).<br>4. Device B receives a file offer. | Device A shows a progress bar reaching 100%. Device B successfully receives, saves, and displays the image in the chat feed. | Device A shows 100% completion. Device B successfully displays the downloaded image in the chat. | Pass | Files are correctly saved to the OS 'Downloads/Stoa' directory. Check that file size matches the original. |
| **Shared Spaces (Groups)** | Create Collaborative Space | 1. Navigate to the Groups tab on Device A.<br>2. Tap "New Group".<br>3. Enter a group name (e.g., "Project Delta") and select Device B to invite.<br>4. Tap "Create". | Device A creates the group and shows it in the list. Device B receives a group invitation popup or notification. | Device A successfully creates the group. Device B receives the invitation notification. | Pass | The group owner is correctly assigned to Device A in the database schema. |
| **CRDT Document Editing** | Real-time Co-editing | 1. Device A and Device B open the same Shared Space document.<br>2. Device A types "Hello " at the beginning of the document.<br>3. *Simultaneously*, Device B types "World" at the end of the document. | Both devices should merge the strokes in real-time without locking the UI. The final document on both devices should read "Hello World" (assuming empty initial state). | Both devices merge the strokes via TCP CRDT deltas. The final document on both screens perfectly matches. | Pass | Vector clocks accurately resolve the sequence. No UI stuttering observed. The Drift Database `crdtStateBlob` successfully updates. |
| **Offline Synchronization** | Offline CRDT Merge | 1. Disconnect Device A from the Wi-Fi network.<br>2. Device A types "Offline edit" into the Shared Space document.<br>3. Reconnect Device A to the Wi-Fi network.<br>4. Wait for the discovery service to re-establish the TCP link. | The edit made offline by Device A should automatically push and sync to Device B upon network reconnection. | Device A's offline edit successfully pushes to Device B as soon as the connection is re-established. | Pass | The app accurately detects network state changes and processes the deferred delta queue. |
